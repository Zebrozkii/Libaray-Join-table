<h1>Library</h1>
<hr />
<h2>Books::</h2>

@if (Model.Count != 0)
{
    <ul>
    @foreach (var item in Model)
    {
        <li><a href='/items/@item.GetId()'>@item.GetBookTitle()</a></li>
    }
    </ul>
}

<h4><a href="/books/new">Add new item</a></h4>
<h4><a href="/">Return home</a></h4>
